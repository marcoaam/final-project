<%= form_for @user, :html => {:multipart => true} do |f| %>

  <%= f.label :bio %>
  <%= f.text_field :bio %>
  <%= f.label :image %>
  <%= f.file_field :image %>

  <%= f.submit "Save" %> 

<% end %>