<% if room.user.nil? %>

	room N°<%= room.number %>
				 <%= link_to "Do you live here ?", room_path(room), method: :patch, class: "live-here-alert" %>
<% else %>
<h2><div class="star-rating-title"><%= "#{star_rating(@property.user.reviews.average(:rating))}" %></div></h2>
	<h2>room N°<%= room.number %></h2>
				 <p>Hello I'm <%= room.user.username %> the tenant living in this room.</p>

         <p>Tenant bio: <%= room.user.bio %></p>
<% end %>