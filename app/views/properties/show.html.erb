 <%= render "properties/slider" %>

 <%= @property.title %>
<div class="row">
 <div class="toggle-vertical col-xs-6 col-md-offset-0">

 	<ul class="nav nav-tabs" role="tablist">
  <li class="active"><a href="#flat-tab" role="tab" data-toggle="tab">Flat</a></li>
  <li><a href="#landlord-tab" role="tab" data-toggle="tab">Landlord</a></li>
  <% if @property.rooms.any?  %>
  	<%= @property.rooms.each do |room| %>
  <li><a href="#room-tab" role="tab" data-toggle="tab">Room <%= room.number %></a></li>
 <% end %>
  <% end %>
</ul>
</div>
<div class="col-xs-6">
<div class="tab-content">
  <div class="tab-pane active" id="flat-tab"><%= render "properties/flat" %></div>
  <div class="tab-pane" id="landlord-tab">LAAAANDLOOOOOORDDDD</div>
  <% if @property.rooms.any?  %>
  	<%= @property.rooms.each do |room| %>
  <div class="tab-pane" id="room-tab">ROOOOOOOOM <%= room.number %></div>
  <% end %>
  <% end %>

</div>
</div>
	

</div>

		<%= @property.rooms.count %> rooms

			<% @property.rooms.each do |room| %>
				room N°<%= room.number %>
				<%= room.description %>
			<% end %>
			Posted by - <%= link_to "#{@property.user.username}", user_path(@property.user) %>
			<%= link_to 'All reviews', property_reviews_path(@property) %>